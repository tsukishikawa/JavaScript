// Dados primitivos: string, number, undefined, null, boolean, symbol 
const nome = 'Mari'; // String
const nome1 = 'Ishikawa'; // String
const nome2 = 'Mariana'; //String
const num1 = 10; // number
const num2 = 12; // number
let nomeAluno; // undefined -> não aponta para local nenhum na memória 
const sobrenomeAluno = null; //Nulo -> não aponta para local nenhum na memória 
const aprovado = false; // Boolean = true or false (lógico)

console.log (typeof nome, nome) // ver o tipo de texto (string e mari)
console.log (typeof num1, num1) // ver to tipo de numero 